import { AcceptAdminInvitationForm } from "@/components/accept-admin-invitation-form";
export default async function AcceptInvitationPage({ searchParams }: { searchParams: Promise<{ token?: string }> }) { const q = await searchParams; return <main className="admin-login-page"><section className="admin-login-card"><div className="admin-login-brand"><span>張</span><div><strong>張曼娟大學堂</strong><small>後台帳號邀請</small></div></div><h1>設定管理帳號密碼</h1><p>密碼至少 12 個字元，並包含文字與數字。完成後使用 Email 登入，系統仍會寄送一次性驗證碼。</p><AcceptAdminInvitationForm token={q.token || ""} /></section></main>; }
